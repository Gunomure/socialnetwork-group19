apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  labels:
    name: grafana-service
    app: demo-voting-app
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30039
  selector:
     app: grafana-pod
